# Valentine App

## Development

### Frontend

Install dependencies:

```sh
pnpm install
```

Start backend and DB using Docker:

```sh
docker compose up db api
```

Run client dev server:

```sh
node --run client:dev
```
